Lab 4 — Static Fields, Properties and Methods
ООП — Клас BoardGame
📌 Опис проєкту

Проєкт демонструє створення та використання статичних полів, властивостей, методів, а також реалізацію Parse, TryParse та перевизначення ToString() у класі BoardGame (настільні ігри).

Програма містить меню, яке дозволяє створювати, переглядати, знаходити та видаляти об’єкти, а також тестувати статичні методи.

📂 Структура проєкту
Lab4/
│
├── Product.cs        // Базовий клас
├── BoardGame.cs      // Основний клас із статичними членами
└── Program.cs        // Меню та взаємодія з користувачем

🎮 Можливості програми
Клас BoardGame реалізує:

властивості гри: назва, жанр, кількість гравців, тривалість, вік, ціна, видавець

перевантажені конструктори

методи виведення (DisplayInfo)

поведінку (PlayDemo)

статичні поля та властивості

статичні методи GetDiscountedPrice, Parse, TryParse

override ToString()

🧩 Статичні поля та властивості
Статичний член	Опис
Counter	Лічильник створених об’єктів
DiscountRate	Глобальна знижка (0–50 %)
ClubName	Назва клубу настільних ігор
🧮 Статичні методи
Метод	Призначення
GetDiscountedPrice(BoardGame)	Обчислює ціну з урахуванням глобальної знижки
Parse(string)	Створює об’єкт із рядка, генерує помилки
TryParse(string, out BoardGame)	Безпечне створення з рядка
📄 Формат рядка для Parse/TryParse
Назва;Жанр;MinPlayers;MaxPlayers;Тривалість;MinAge;Ціна;Видавець


Приклад:

Каркассон;Стратегія;2;5;60;8;800;Hobby World

🔍 Приклади використання
Parse():
string data = "Монополія;Економічна;2;6;90;8;1000;Hasbro";
BoardGame game = BoardGame.Parse(data);

TryParse():
if (BoardGame.TryParse(data, out BoardGame g))
{
    Console.WriteLine("Успішно створено!");
}

Статичний метод:
BoardGame.DiscountRate = 0.15m;
decimal discounted = BoardGame.GetDiscountedPrice(game);

📜 Меню програми
1 – Додати гру
2 – Переглянути всі ігри
3 – Знайти гру
4 – Продемонструвати поведінку
5 – Видалити гру
6 – Продемонструвати static-методи
0 – Вийти
